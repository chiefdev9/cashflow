---
/* 
   FILE: index.astro
   DESKRIPSI: Dashboard Cashflow Tanaejka (Membaca Tabel 'summary' & Realtime Supabase).
*/

import { Image } from 'astro:assets';
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="id">
<head>
    <link rel="manifest" href="/site.webmanifest" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>Cashflow Tanaejka</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-100 text-slate-800 flex justify-center items-center min-h-dvh p-0 sm:p-4 overflow-hidden">

    <!-- Frame Mobile / App Container -->
    <div class="w-full sm:max-w-md h-dvh sm:h-[844px] bg-slate-50 shadow-2xl sm:rounded-3xl flex flex-col overflow-hidden relative border border-slate-200">
        
        <!-- HEADER & CARD RINGKASAN -->
        <header class="bg-white p-4 sm:p-5 pb-5 sm:pb-6 rounded-b-[2.5rem] shadow-sm border-b border-slate-100 relative overflow-hidden flex-shrink-0 z-10">
            <!-- Background Image Accent -->
            <div class="absolute inset-0 opacity-30 pointer-events-none overflow-hidden flex items-center justify-center">
                <Image 
                    src="/bg.jpg" 
                    alt="Aksen Vektor Header" 
                    width={800}
                    height={600}
                    class="w-full h-full object-cover scale-125 transform"
                />
            </div>

            <!-- Profile Info & Bell -->
            <div class="flex justify-between items-center mb-3 sm:mb-4 relative z-10">
                <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 rounded-full bg-slate-100 border border-slate-200/80 flex items-center justify-center font-bold text-slate-700 shadow-xs text-xs">
                        ST
                    </div>
                    <div>
                        <p class="text-[10px] sm:text-[11px] font-bold tracking-wider text-slate-500 uppercase">Cashflow Tanaejka</p>
                        <h1 class="text-xs sm:text-sm font-extrabold text-slate-800 leading-tight">Agustus 2026</h1>
                    </div>
                </div>
                <button class="w-9 h-9 rounded-full bg-slate-50/80 backdrop-blur-sm border border-slate-200/80 flex items-center justify-center text-slate-600 hover:bg-slate-100 transition shadow-xs">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
                    </svg>
                </button>
            </div>

            <!-- Card Utama Ringkasan Data (Membaca Tabel 'summary') -->
            <div class="bg-white/90 backdrop-blur-md border border-slate-200/80 p-3.5 sm:p-4 rounded-2xl shadow-xs space-y-3 relative z-10">
                
                <!-- Row Top Metrics -->
                <div class="grid grid-cols-3 gap-2 text-center items-center">
                    <div>
                        <span class="block text-[9px] sm:text-[10px] font-bold text-slate-400 uppercase tracking-wider">Perputaran</span>
                        <span id="txtPerputaran" class="text-xs sm:text-sm font-bold text-slate-800">Rp 0</span>
                    </div>
                    <div class="border-x border-slate-100 px-1">
                        <span class="block text-[9px] sm:text-[10px] font-bold text-slate-400 uppercase tracking-wider">Sisa Bulanan</span>
                        <span id="txtSisaBulanan" class="text-xs sm:text-sm font-bold text-slate-800">Rp 0</span>
                    </div>
                    <div>
                        <span class="block text-[9px] sm:text-[10px] font-bold text-slate-400 uppercase tracking-wider">Saldo Akhir</span>
                        <span id="txtSaldoAkhir" class="text-xs sm:text-sm font-bold text-emerald-600">Rp 0</span>
                    </div>
                </div>

                <!-- Row In / Out Metrics -->
                <div class="grid grid-cols-2 gap-2 pt-1">
                    <div class="bg-slate-50/80 p-2 sm:p-2.5 rounded-xl border border-slate-100">
                        <span class="block text-[9px] sm:text-[10px] font-semibold text-slate-400">Pemasukan (In)</span>
                        <span id="txtPemasukan" class="text-xs sm:text-sm font-bold text-emerald-600">Rp 0</span>
                    </div>
                    <div class="bg-slate-50/80 p-2 sm:p-2.5 rounded-xl border border-slate-100">
                        <span class="block text-[9px] sm:text-[10px] font-semibold text-slate-400">Pengeluaran (Out)</span>
                        <span id="txtPengeluaran" class="text-xs sm:text-sm font-bold text-rose-600">Rp 0</span>
                    </div>
                </div>

            </div>
        </header>

        <!-- MAIN CONTENT: DUA TAB (RIWAYAT & STATISTIK) -->
        <main class="flex-1 overflow-y-auto p-4 pb-32">
            <!-- Pilihan Tab Navigation -->
            <div class="flex border-b border-slate-200 mb-4 space-x-6 text-xs font-bold">
                <button id="tabRiwayat" class="pb-2 border-b-2 border-slate-800 text-slate-800 transition">Riwayat Transaksi</button>
                <button id="tabStatistik" class="pb-2 border-b-2 border-transparent text-slate-400 hover:text-slate-600 transition">Statistik</button>
            </div>

            <!-- Konten Tab 1: Riwayat Transaksi -->
            <section id="contentRiwayat" class="space-y-2">
                <div id="loadingState" class="text-center py-8 text-xs text-slate-400">
                    Memuat transaksi...
                </div>
                <div id="listTransaksi" class="space-y-2"></div>
            </section>

            <!-- Konten Tab 2: Grafik/Statistik -->
            <section id="contentStatistik" class="hidden">
                <div class="bg-white p-4 rounded-2xl border border-slate-100 shadow-sm text-center">
                    <p class="text-xs font-semibold text-slate-500">Statistik Bulan Agustus 2026</p>
                    <div class="h-40 flex items-center justify-center text-slate-300 text-xs">
                        [Grafik Statistik Supabase]
                    </div>
                </div>
            </section>
        </main>

        <!-- FAB (Floating Action Button) Tambah Transaksi -->
        <div class="absolute bottom-6 left-0 right-0 px-6 flex justify-center z-30 pointer-events-none">
            <a href="/out" class="pointer-events-auto bg-slate-900 hover:bg-slate-800 text-white font-medium py-3 px-6 rounded-full shadow-xl text-xs sm:text-sm transition transform active:scale-95 flex items-center space-x-2 border border-slate-700">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                </svg>
                <span>Tambah Transaksi</span>
            </a>
        </div>

    </div>

    <!-- SCRIPT REALTIME SUPABASE INTEGRATION -->
    <script>
        import { createClient } from '@supabase/supabase-js';

        const supabaseUrl = 'https://qcmfltewnmtvpswthbcj.supabase.co';
        const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFjbWZsdGV3bm10dnBzd3RoYmNqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODY5MzEyMjEsImV4cCI6MjEwMjUwNzIyMX0.gj5rUMELfGeyIOtmWepN-rekU1-lubX1ll-zyGHzVCU';
        
        const supabase = createClient(supabaseUrl, supabaseKey);
        const TARGET_BULAN = 'Agust';

        // Helper Format Rupiah
        function formatRupiah(val: number | null | undefined): string {
            if (val === null || val === undefined) return 'Rp 0';
            const isNegative = val < 0;
            const formatted = Math.abs(val).toLocaleString('id-ID');
            return isNegative ? `-Rp ${formatted}` : `Rp ${formatted}`;
        }

        // 1. FUNGSI FETCH SUMMARY DARI TABEL 'summary'
        async function fetchSummary() {
            const { data, error } = await supabase
                .from('summary')
                .select('*')
                .eq('Bulan', TARGET_BULAN)
                .single();

            if (error) {
                console.error('Gagal mengambil data summary:', error);
                return;
            }

            if (data) {
                const elPerputaran = document.getElementById('txtPerputaran');
                const elSisaBulanan = document.getElementById('txtSisaBulanan');
                const elSaldoAkhir = document.getElementById('txtSaldoAkhir');
                const elPemasukan = document.getElementById('txtPemasukan');
                const elPengeluaran = document.getElementById('txtPengeluaran');

                if (elPerputaran) elPerputaran.innerText = formatRupiah(data.Perputaran);
                
                if (elSisaBulanan) {
                    elSisaBulanan.innerText = formatRupiah(data.Sisa_Bulanan);
                    // Warna teks merah jika minus
                    if (data.Sisa_Bulanan < 0) {
                        elSisaBulanan.className = 'text-xs sm:text-sm font-bold text-rose-600';
                    } else {
                        elSisaBulanan.className = 'text-xs sm:text-sm font-bold text-slate-800';
                    }
                }

                if (elSaldoAkhir) elSaldoAkhir.innerText = formatRupiah(data.Sisa_Akhir);
                if (elPemasukan) elPemasukan.innerText = formatRupiah(data.Pemasukan);
                if (elPengeluaran) elPengeluaran.innerText = formatRupiah(data.Pengeluaran);
            }
        }

        // 2. FUNGSI FETCH RIWAYAT TRANSAKSI DARI TABEL 'transaksi'
        async function fetchTransaksi() {
            const listEl = document.getElementById('listTransaksi');
            const loadingEl = document.getElementById('loadingState');

            const { data, error } = await supabase
                .from('transaksi')
                .select('*')
                .order('created_at', { ascending: false });

            if (loadingEl) loadingEl.style.display = 'none';

            if (error) {
                console.error('Gagal mengambil riwayat transaksi:', error);
                if (listEl) listEl.innerHTML = `<p class="text-xs text-center text-rose-500">Gagal memuat riwayat.</p>`;
                return;
            }

            if (listEl && data) {
                if (data.length === 0) {
                    listEl.innerHTML = `<p class="text-xs text-center text-slate-400 py-4">Belum ada transaksi.</p>`;
                    return;
                }

                listEl.innerHTML = data.map((t: any) => {
                    const isIn = t.jenis === 'In';
                    const amountFormatted = (isIn ? '+ ' : '- ') + formatRupiah(t.angka);
                    const textColor = isIn ? 'text-emerald-600' : 'text-rose-600';
                    const badgeBg = isIn ? 'bg-emerald-50 text-emerald-600' : 'bg-rose-50 text-rose-600';

                    return `
                        <div class="bg-white p-3 rounded-2xl border border-slate-100 shadow-xs flex justify-between items-center">
                            <div class="flex items-center space-x-3">
                                <div class="w-8 h-8 rounded-xl ${badgeBg} flex items-center justify-center font-bold text-xs">
                                    ${isIn ? 'IN' : 'OUT'}
                                </div>
                                <div>
                                    <h3 class="text-xs font-bold text-slate-800">${t.kategori || 'Transaksi'}</h3>
                                    <p class="text-[10px] font-medium text-slate-400">${t.sumber || ''} • ${t.catatan || ''}</p>
                                </div>
                            </div>
                            <div class="text-right">
                                <span class="block text-xs font-bold ${textColor}">${amountFormatted}</span>
                                <span class="text-[9px] text-slate-400">${t.waktu ? t.waktu.split('T')[0] : ''}</span>
                            </div>
                        </div>
                    `;
                }).join('');
            }
        }

        // 3. LISTEN TO SUPABASE REALTIME (Update Otomatis saat Tabel Berubah)
        supabase
            .channel('db-changes')
            .on('postgres_changes', { event: '*', schema: 'public', table: 'summary' }, () => {
                fetchSummary();
            })
            .on('postgres_changes', { event: '*', schema: 'public', table: 'transaksi' }, () => {
                fetchTransaksi();
            })
            .subscribe();

        // 4. LOAD INITIAL DATA
        fetchSummary();
        fetchTransaksi();

        // 5. TAB SWITCHING LOGIC
        const tabRiwayat = document.getElementById('tabRiwayat');
        const tabStatistik = document.getElementById('tabStatistik');
        const contentRiwayat = document.getElementById('contentRiwayat');
        const contentStatistik = document.getElementById('contentStatistik');

        tabRiwayat?.addEventListener('click', () => {
            contentRiwayat?.classList.remove('hidden');
            contentStatistik?.classList.add('hidden');
            tabRiwayat.className = 'pb-2 border-b-2 border-slate-800 text-slate-800 transition';
            tabStatistik!.className = 'pb-2 border-b-2 border-transparent text-slate-400 hover:text-slate-600 transition';
        });

        tabStatistik?.addEventListener('click', () => {
            contentStatistik?.classList.remove('hidden');
            contentRiwayat?.classList.add('hidden');
            tabStatistik.className = 'pb-2 border-b-2 border-slate-800 text-slate-800 transition';
            tabRiwayat!.className = 'pb-2 border-b-2 border-transparent text-slate-400 hover:text-slate-600 transition';
        });
    </script>
</body>
</html>